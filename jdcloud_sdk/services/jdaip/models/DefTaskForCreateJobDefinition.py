# coding=utf8

# Copyright 2018 JDCLOUD.COM
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This class is auto generated by the jdcloud code generator program.


class DefTaskForCreateJobDefinition(object):

    def __init__(self, taskType, appGroupId, periodicType, description=None, managers=None, cycleType=None, day=None, hour=None, minute=None, cycleStep=None, cronExpression=None, nextTime=None, effectiveTime=None, slaTime=None, failRetry=None, retryTimes=None, retryInterval=None, selfDepend=None, concurrency=None, parentRerunPolicy=None, tagIdList=None, monitorStatus=None, status=None):
        """
        :param taskType:  任务类型。枚举类型，取值范围：
- `single`：标准任务
- `wf`：工作流任务

        :param description: (Optional) 任务描述。长度[0,512]

        :param appGroupId:  任务所属项目空间ID。正确应用ID，操作用户必须具有当前项目空间权限

        :param managers: (Optional) 任务负责人，多个用逗号分割。默认接口调用用户erp（userToken解密后），负责人必须具有当前项目空间权限（任务修改时，不支持此字段修改）

        :param periodicType:  调度方式。枚举类型，取值范围：
- `cycle`：周期任务
- `temp`：临时任务
- `manual`：手动任务

        :param cycleType: (Optional) 周期类型。周期任务（periodicType="cycle"）必填。枚举类型，取值范围：
- `quarter`：季度
- `month`：月
- `week`：周
- `day`：天
- `hour`：小时
- `minute`：分钟
- `custom`：自定义

        :param day: (Optional) 执行时间的天。
月周期任务（periodicType="cycle"，cycleType="month"）和周周期任务（periodicType="cycle"，cycleType="week"）必填。
月任务取值范围：1-32（1日-31日，32代表月末），周任务取值范围：1-7（周一-周日）

        :param hour: (Optional) 执行时间的小时。月/周/天周期任务必填。取值范围0-23正整数

        :param minute: (Optional) 执行时间的分钟。月/天/周/小时周期任务必填。取值范围0-59正整数

        :param cycleStep: (Optional) 周期步长。分周期任务（periodicType="cycle"，cycleType="minute"）必填。范围：1,5,10,15,30

        :param cronExpression: (Optional) 自定义运行规则。自定义周期任务（periodicType="cycle"，cycleType="custom"）必填。
格式: [秒] [分] [小时] [日] [月] [周] [年]
举例：0 0 12 * * ? 每天12点触发；0 * 14 * * ? 每天下午2点到2点59分每分触发

        :param nextTime: (Optional) 执行时间。周期任务（periodicType="temp"）必填。时间格式：yyyy-MM-dd HH:mm:ss

        :param effectiveTime: (Optional) 任务生效时间。周期任务（periodicType="cycle"）有效。
任务生成实例的开始时间，格式（YYYY-MM-DD HH:mm:ss）。
1. 若该时间早于当前时间，将补齐该时间至当前时间内的实例
2. 若该时间晚于当前时间，将从该时间开始生成实例
3. 该时间保存后不可修改

        :param slaTime: (Optional) 预计完成时间
        :param failRetry: (Optional) 失败是否重试。枚举类型，1=重试，0=不重试。默认开启，重试1次，重试间隔30s

        :param retryTimes: (Optional) 失败重试次数。失败重试开启（failRetry=1）时必填，默认1。
未指定失败是否重试时（failRetry=null），默认值有效。取值范围：1-10

        :param retryInterval: (Optional) 失败重试间隔(秒)。失败重试开启（failRetry=1）时必填，默认30。
未指定失败是否重试时（failRetry=null），默认值有效。取值范围：0-36000

        :param selfDepend: (Optional) 实例执行策略。默认0。取值范围：
- 0：不依赖上一周期
- 1：依赖上一周期
- 2：只运行最新周期

        :param concurrency: (Optional) 同一任务允许同时执行实例数。默认5。可选值：1,2,3,5,10,15,20

        :param parentRerunPolicy: (Optional) 跟随父任务重跑策略。默认0。可选值：
- 0：不通知任务负责人，不自动重跑
- 1：通知任务负责人，不自动重跑
- 3：通知任务负责人，自动重跑（正在执行时除外）

        :param tagIdList: (Optional) 标签id列表
        :param monitorStatus: (Optional) 默认监控开关。可选值：
- 1：开启默认监控
- 2：关闭默认监控

        :param status: (Optional) 任务状态。1=测试任务，3=线上任务

        """

        self.taskType = taskType
        self.description = description
        self.appGroupId = appGroupId
        self.managers = managers
        self.periodicType = periodicType
        self.cycleType = cycleType
        self.day = day
        self.hour = hour
        self.minute = minute
        self.cycleStep = cycleStep
        self.cronExpression = cronExpression
        self.nextTime = nextTime
        self.effectiveTime = effectiveTime
        self.slaTime = slaTime
        self.failRetry = failRetry
        self.retryTimes = retryTimes
        self.retryInterval = retryInterval
        self.selfDepend = selfDepend
        self.concurrency = concurrency
        self.parentRerunPolicy = parentRerunPolicy
        self.tagIdList = tagIdList
        self.monitorStatus = monitorStatus
        self.status = status
