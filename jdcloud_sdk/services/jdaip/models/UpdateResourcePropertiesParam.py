# coding=utf8

# Copyright 2018 JDCLOUD.COM
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This class is auto generated by the jdcloud code generator program.


class UpdateResourcePropertiesParam(object):

    def __init__(self, imageSource=None, imageId=None, lbSpec=None, workloadSpec=None, storages=None, datasets=None, models=None, codes=None, envs=None, nodeAffinities=None, rdma=None, sshSpec=None, taskPriority=None):
        """
        :param imageSource: (Optional) 镜像来源，指定新镜像的来源类型。

## 来源类型
- public: 公共镜像
- self: 自定义镜像

## 使用说明
- 参数`imageSource`和`imageId`必须同时传或者都不传

        :param imageId: (Optional) 镜像ID，新镜像的唯一标识。

## 使用说明
- 参数`imageSource`和`imageId`必须同时传或者都不传
- 用于指定要更换的镜像

        :param lbSpec: (Optional) 负载均衡配置，用于更新公网访问能力。

## 使用说明
- 私有资源池中的Notebook: 需要传入与资源队列可通信的负载均衡ID和端口
- 公共资源池中的Notebook: 可通过lbEnable=true开启公网访问，系统自动分配LB
- 不需要公网访问时不要指定此参数

        :param workloadSpec: (Optional) 工作负载资源配置，用于更新计算资源。

## 更新限制
- 公共/专享资源池不允许变更资源配置
- 私有资源池允许更换队列和资源配置

        :param storages: (Optional) 存储空间配置，用于更新存储挂载。

## 使用说明
- 更新存储配置需要确保新配置与队列的网络连通性
- 第一个存储仍需挂载到`/mnt/workspace`

        :param datasets: (Optional) 数据集配置，用于更新数据集挂载。

## 使用说明
- 更新数据集配置需要确保新配置与队列的网络连通性

        :param models: (Optional) 模型配置，用于更新模型挂载。

## 使用说明
- 更新模型配置需要确保新配置与队列的网络连通性

        :param codes: (Optional) 代码配置，用于更新代码库挂载。

## 使用说明
- 传null表示不修改此属性
- 传空数组表示清空代码配置

        :param envs: (Optional) 用户环境变量列表。

## 更新语义
- 该参数可选
- 不传或传null表示不修改原配置
- 传空数组表示清空全部用户环境变量
- 传非空数组表示全量替换原配置
- 数量、名称和变量值限制与创建Notebook时一致

        :param nodeAffinities: (Optional) 节点亲和性配置，用于更新Pod调度规则。

## 使用说明
- 支持配置多个亲和性规则，每个元素之间是"或"的关系
- 传null表示不修改此属性
- 传空数组表示清空亲和性配置

        :param rdma: (Optional) 是否启用RDMA高速网络。

## 更新语义
- 不传或传`null`表示不修改原配置
- 传`true`表示开启，传`false`表示关闭；配置在Notebook下次启动时生效

## 生效规则
- 仅私有资源池中的Notebook支持修改该参数
- 计算资源必须包含GPU，且GPU卡数必须为正整数
- 未配置GPU或配置非整卡GPU时，即使传`true`也会静默按`false`处理
- 公共/专享资源池忽略该参数，是否启用由所选规格的RDMA属性决定

        :param sshSpec: (Optional) SSH配置，用于更新SSH连接能力。

## 使用说明
- 传null表示不修改此属性

        :param taskPriority: (Optional) 任务调度优先级，数值越大，优先级越高。

## 使用说明
- 可选参数，传null表示不修改（保持原值）
- **取值范围：** 1 ~ 9
- **按资源池类型区分：**
  - 专享队列：必须设置(1~9)。
  - 空间私有队列：开启调度优先级时必填。
  - 公共队列：不支持设置。

        """

        self.imageSource = imageSource
        self.imageId = imageId
        self.lbSpec = lbSpec
        self.workloadSpec = workloadSpec
        self.storages = storages
        self.datasets = datasets
        self.models = models
        self.codes = codes
        self.envs = envs
        self.nodeAffinities = nodeAffinities
        self.rdma = rdma
        self.sshSpec = sshSpec
        self.taskPriority = taskPriority
